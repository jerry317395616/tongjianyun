# 食谱到 ERPNext 采购需求（第一阶段）

## 批量处理未匹配食材

在“核对食材与备餐人数”弹窗点击“批量处理未匹配食材”。
选择本批新物料分类，逐项核对“关联已有 / 新建物料 / 暂不处理”，预览后确认保存。
预览不写入任何数据；确认后通过标准 Item.insert 建档，并在既有采购记录 JSON 中保存
独立类型 erp_recipe_mapping 的确认凭据。整个批次使用同一事务，无忽略权限。
重复请求返回原结果；同名物料、无效分类/单位、未确认的汤粥菜品均拦截。

常见别名目前是显式的小型候选表（豆干/豆腐干、番茄/西红柿），不是 AI 识别结论。
相似名称仅供选择，不默认关联；芽、酱、糁、棒、汤、粥、粒等形态差异不推荐合并。
汤粥类需要明确确认是外购成品，自制菜品应返回食谱拆分配方；不能静默遗漏采购需求。
新建记录不自动产生库存、采购单或费用，也不会修改食谱日期。

单元测试补充：`env/bin/python -m unittest tongjianyun.tests.test_ingredient_resolution`。

所有实现均在 tongjianyun，不修改上游代码或 DocType 结构。

入口：食谱工作台 → 打开已发布食谱 → 准备 ERP 采购需求。
选择公司与有效收货仓库；核对物料匹配、各餐人数、采购毛料换算系数；
预览按天/物料汇总的总需求；确认创建 Material Request（Purchase）草稿。

## 范围

- 同名物料只作为候选，歧义不自动选；从账号可读的最近 100 条集成采购记录复用同公司已确认映射。
- 以食材名称与原单位构成稳定映射键，不依赖食谱保存时重建的明细编号。
- 全园已确认就餐人数仅为提示，需人工确认适用班级/餐次范围；空人数不能推定为在册人数。
- 毛料换算系数由使用者确认；质量与体积之间不猜密度、包装不猜规格。
- 新物料使用 ERPNext 原生快速录入，尊重 Item 创建权限。
- 采购追溯复用 Tongjianyun Food Purchase 既有 JSON 字段，独立 record_type=erp_recipe_request。
  不调用原有 storage.py 中绕过权限的接口。
- 一份食谱、一个公司仅生成一个集成需求。相同请求重试打开原单；参数变化拒绝覆盖或重复创建。
  撤销、部分采购后的差额修订不在本阶段自动执行，需人工核对并采用明确修订流程。
- 来源快照、确认人数、映射、草稿引用一起保存；标准 ORM 事务，无手动提交、无忽略权限。

## 不做什么

不新增 UOM/Item Group 默认资料；不自动提交采购、扣库存、记费用或付款；
不推算可食率、不扣除库存/在途订单；本版是总需求，不是净采购量。
不创建 BOM；不把菜品临时调整升级为标准配方。
不自动同步 ERP 审批状态到旧采购界面；以 ERP 单据的实际状态为准。

## 校验

源食谱、菜品/食材明细、物料、公司、仓库均需可读；创建需 Material Request create、
Tongjianyun Food Purchase create/write/read 权限。未配置权限时拒绝，不自动提权。
库存单位必须存在，物料必须启用且可采购、维护库存；整数单位禁止小数需求。
食谱含过去日期、空需求、未匹配项、单位变化及预览后来源变化均阻止创建。

## 测试

`env/bin/python -m unittest tongjianyun.tests.test_recipe_procurement`

仅明确需要时运行回滚验证（使用 child.myyr.top 当前配置，不修改现有业务记录）：
`env/bin/python -m tongjianyun.tests.check_recipe_procurement_live`

该脚本创建临时 Item、食谱、菜品、食材、采购追溯及 Material Request，验证标准草稿创建、
幂等重试、过期预览拒绝、历史映射复用与访客拒绝；最后 rollback 并核对没有残留。
