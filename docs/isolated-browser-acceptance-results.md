# 统一场景浏览器真实保存验收

2026-09-25；仅专用 `unified-business-acceptance.localhost:23380`，经 loopback SSH 转发访问。
使用独立合成管理账号，不复制生产登录、不触发真实 Codex、不声称是普通教师角色验收。

## 实际完成

在右侧历史会话中的合成导航按钮打开左侧真实业务组件。导航由原 `publish_for_task`
校验、发布，明确标注“隔离测试导航，未调用Codex”。不是调用智能体，也不是实时 SSE
端到端验收；业务查看和保存使用正常网页请求及原业务服务。

班级 `QA Teacher 9680e04d9f Assigned`、日期 `2026-09-18`，两名合成学生：

1. 默认打开周食谱，点击右侧“查看：班级当日出勤”切换左侧点名表。
2. 初始两人均待登记，保存禁用。只选择第一人到园，保存启用；实际点击保存后
   服务回读第一人 Present、第二人 Unknown，统计出勤1/未登记1，不把未登记算缺勤。
3. 点击右侧本餐导航，午餐两人均未确认，预计列为预计就餐；确认禁用。
4. 点击“本班本餐全部就餐”仅填草稿，再将第二人改不就餐，点击确认保存。
   页面回读一人就餐、一人不就餐，出现已确认记录的修改原因输入框。
5. 第二人改为就餐：未填原因时确认禁用；填写
   `隔离浏览器验收：核对后补录本餐实际就餐` 后启用并保存。
6. 刷新页面后重新点击两个业务按钮，直接读取 DOM 选中值：午餐为就餐/就餐，
   考勤仍为 Present/Unknown。右侧会话持续保留，未发送聊天任务。

## 独立数据库回读

新连接使用 `START TRANSACTION READ ONLY`，完成18项核验并rollback。
私有证据：

`/home/zyd/frappe/remote-workspace/tgy-blueprint-lifecycle-qa-20260925/browser-readback-ad5c578350.json`

- 午餐实际2，其他四餐实际null、逐生未确认；班级和全日仍待确认。
- 考勤只新增第一人到园，不把第二人补成到园或缺勤。
- 预计安排与原规则一致；Version未记录预计字段变化。保存前预计为2的证据来自浏览器，
  不将保存后的数据库读取冒充保存前数据库快照。
- Version保留不就餐到就餐变更、精确原因及合成管理账号；原生核对Comment也有正确操作者。
- 未修改生产数据、未启动worker/scheduler、未创建真实Codex任务。

## 修正的测试环境问题

首次复制的候选静态目录较旧，缺少考勤/用餐组件，页面明确提示“需要更新页面组件”，
没有假装已经切换。生产文件当时已有组件，不能把此问题归为生产缺陷。

新增 `serve_isolated_browser.py refresh-assets`，只刷新公开资源，不重置账号、口令、
导航或数据。校验1295份源文件/副本以及HTTP实际关键资源SHA256，检查必需组件和
chat/views/template版本一致。防火墙拒绝智能体执行、上传及非验收RPC，包括GET body
的cmd变体；14项守卫测试通过。

## 尚未通过的部分

- 未保存切换会调用浏览器原生确认框；这次内嵌浏览器控制通道在弹窗阶段超时，
  无法完成取消/关闭动作。该隔离标签仅有未提交草稿，另开标签确认未被误保存，
  之后完成上述保存主线。**不计作离开保护真实浏览器通过**；既有组件测试仍通过。
- 普通教师统一场景访问、失联/陈旧版本/锁定的真实浏览器反例、蓝图启用及原生表单
  写入、实时Codex到SSE的完整测试尚待继续，不由本次管理账号正向流程替代。
- 库存修复仍限指定隔离环境；本批浏览器没有执行库存或财务写入。

## 本轮收尾

已按精确PID、zyd身份、命令路径核对后停止本轮QA Web，确认23380不再监听；
Windows本轮SSH转发也已关闭。已完成保存的临时标签已关闭；确认框阻塞的临时标签
未能通过浏览器控制接口关闭，没有继续绕过控制通道操作。未触及用户生产标签。
保留隔离数据库、Redis、合成业务记录及0600回读证据，便于下一批复核。

## 第五批：v2 新业务真实浏览器启用与草稿保存

2026-09-25，同一隔离站点和合成管理账号。通过真实 `propose_for_task` 准备的私有方案
`888d4f4ca2` 绑定任务 `f5e5899a-e1ef-40a0-b04d-16de118e7282`；导航明确标注未调用
Codex。准备过程不创建 DocType 或业务记录。

浏览器实际执行：

1. 点击会话导航，查看主表、明细、固定计算及原生复核流程。
2. 点击“确认启用这项业务”，先“暂不启用”；页面仍为未创建结构的方案。
3. 再打开内联确认区并点击“确认创建以上业务结构”，按钮等待期间禁用；成功回执绑定
   proposal/revision/doctype，随后重新 GET 方案，显示实际结构已启用。
4. 点击“开始录入”，在左侧原生表单填写名称“浏览器合成活动估算”和两条合成明细：
   测试材料 A，2.5 × 3.8；测试材料 B，10 × 1.25。误添的未保存空行在草稿中移除。
5. 真实点击 Save，保存为 `pespg1lt48`，状态“扩展·草稿”；小计回算为 9.50、12.50，
   合计 22.00。原生 Menu → Reload 后，页面仍显示两条明细及相同计算值。

此次验证不是实际采购或付款，也没有送审/复核。QA 全局币种尚未配置，原生界面显示
默认 ₹ 符号；只验证数值计算和两位小数，**不将它当作正确币种或多币种财务验收**。

### 测试环境问题与边界

QA 原生 Desk 最初因安装完成标记缺失而重定向安装向导。严格隔离守卫核对合成公司、
科目、仓库后，仅补齐 frappe/erpnext 的 Installed Application 完成标记和 System
Settings.setup_complete；旧新值保留于私有 `browser-desk-readiness.json`。没有运行
完整安装向导、改角色/口令或生产配置。额外放行精确只读启动 RPC，安装向导写入口仍禁止。

CUA `fill` 在该原生控件上曾只更新输入框而未写入表单模型，改为实际聚焦/键盘输入并核对
草稿后才保存；不调用页面内业务方法代替点击。QA 屏障还拦截了原生附带 RPC并显示权限
提示；保存本身已通过，附带请求的定位与处理单独记录，不把提示掩盖为完整无错误链路。

独立新连接 `START TRANSACTION READ ONLY` 回读 **19/19** 通过，证据为 QA root 下
`browser-blueprint-readback-5df09fc541.json`（0600）。验证唯一记录、真实管理账号 owner/
modified_by、两行 parent 绑定、草稿状态、服务器计算值及完整类型/工作流清单。回读脚本
不激活结构、不保存业务。附带权限提示已定位为 QA 对只读 `get_transitions` 请求的草稿
字段检查过严，不能把它说成业务权限配置错误。

后续原生 Reload 的 getdoc/get_transitions 返回200；更精确的只读临时单据身份边界与
严格Save payload边界分开。第二次独立回读 `browser-blueprint-readback-4e38ef3652.json`
仍19/19，补充币种限制说明。另一附带权限提示经源码/请求摘要定位为被QA屏障拒绝的
后台任务列表查询；保持拒绝，没有因此开放后台任务执行、重试或取消。

## 第五批：真实普通教师同场景保存

仍是同一隔离站点，使用原有 `Instructor + Academics User` 合成教师，通过正常登录
切换身份；未添加管理员角色、临时DocPerm或登录绕过。目标班级仍为前述Assigned班，
改用事先确认无记录且未锁定的 `2026-09-17` 午餐，不覆盖上一批9月18日管理者证据。

1. 打开统一场景，默认显示本人班级点名；左侧仅少量业务导航/日期餐次，右侧明确
   “业务对话尚未开通”，没有聊天输入、历史或SSE冒充教师Codex。
2. 第一人选择到园，第二人保持待登记；点击保存，回读出勤1、未知1。
3. 场景内切到核对用餐，第一人就餐、第二人不就餐，点击“确认本餐”。
4. 整页刷新后再次读取：考勤仍为Present/Unknown，午餐选中值仍为就餐/不就餐。
5. 学生视图和可用业务目录在左侧正常切换，保留9月17日/午餐上下文；没有服务器项目
   动作，目录明确普通账号的Report/Page/Workspace仍待范围验收。

操作前独立baseline：`teacher-browser-baseline-20260917.json`；操作后新连接READ ONLY
报告：`teacher-browser-readback-4e8efeebdf.json`，**24/24**。两者位于QA root，0600。
核验真实教师owner/modified_by与午餐Comment，只有一条原生考勤；实际午餐1，其他四餐
仍Unknown；预计字段与baseline一致，20份跨班/其他日期/名册记录hash未变；角色不变，
日汇总、root聊天和项目目录仍拒绝。诊断审计由受信验收脚本只读检查，不赋予教师额外读取权。

本次还发现QA先后加载了不同内容的同名v5资源，HTTP源文件摘要一致不能证明浏览器没有
使用缓存。已将合并版chat/views/template升级为 `unified-business-20260925-6`，周历入口
为 `teacher-scene-20260925-1`，再正常刷新完成上述验收。QA登录退出的无Content-Type
空body兼容仅允许当前会话无参数logout；bootstrap只接受day/meal/group，无actor/role。

仍未验收：普通教师真实Codex隔离/SSE、教师浏览器跨班/失联/并发负例和全部专业业务。
这些不由上述24项回读、343项后端或127项前端测试替代。
