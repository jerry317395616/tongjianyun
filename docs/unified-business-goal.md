# 统一业务场景实施与验收账本

## 核心目标

用户在右侧表达需求，Codex 处理项目与业务，左侧完成业务查看和办理。默认仍为周食谱。
覆盖范围是服务器 `/home/zyd/frappe` 的项目，不仅是童健云；站点账号和业务权限不跨项目冒用。
新需求优先复用已安装业务，确实缺少时可创建新业务，不能仅回复“不支持”。

## 当前基线（2026-09-25）

当前只有一个 Native Bench 站点 `child.myyr.top`，安装 7 应用、47 模块。
实时只读路由检查：662 主单据类型、233 报表、24 页面、38 工作区，共 957 入口，路由生成失败 0。
根目录索引 31 个项目/目录；包含备份、配置和工具源码，**不等于 31 个可办理业务系统**。

## 本批实现

- 常用列表和明细接入原生办理，权限允许时新建或编辑，保留原服务校验、审批、并发与审计。
- `frappe_new` 可在场景内打开新单据。周食谱排除通用新建，避免重复周食谱。
- `capability_inventory()` 逐入口提供可用权限、需要单据、状态条件与未验收标记。
- 原生表单在聊天结束时保活；离开未保存表单必须确认。原生导航同步当前单据上下文。
- 展示请求与实际浏览器渲染区分；模型不再把 SSE 发出当作已展示/已保存。
- `business_blueprint`：Codex 生成安全数据方案，私有文件持久化，左侧预览，摘要绑定确认，再创建独立 Custom DocType。
- 新类型自动进入当前站点业务目录，可列表、新建、编辑，启用历史；初始仅 System Manager 权限，不授权删除。
- 不接受模型 HTML、脚本、任意 SQL、字段表达式、自动默认值或权限元数据。

## 未知业务边界

简单登记业务可通过方案直接创建。已保存方案不等于已启用；启用不等于已录入记录。
复杂子表、财务过账、库存动作、外部系统、专业审批需要源码扩展、权限测试和部署验证，不能由简单字段表冒充。
新建表使用 Frappe 原生 ORM 的 schema 同步，不开放通用 DDL，不覆盖已有类型，不自动迁移/删除历史数据。
DDL 不能保证事务回滚；失败必须核对结构。重复启用仅接受相同结构，不能删表重试。

## 完整验收仍需继续

| 领域 | 接入方法 | 完整业务流程验收 |
| --- | --- | --- |
| 周食谱与营养 | 专用周历/营养视图、原食谱服务 | 既有编辑去重已验证；继续回归 |
| 学生、班级、出勤、健康 | 专用汇总 + 原生办理 | 逐角色办理/回读待验收 |
| 实际用餐 | 班级分餐确认 + 日汇总自动完成 | 已实现；隔离管理员真实流程通过，教师正向流程及浏览器保存待验收 |
| 采购、收货、库存 | 专用视图 + ERPNext 原生办理 | 下单→收货→库存完整链待验收 |
| 财务、资产、HR、教育等其余已安装业务 | 元数据发现 + 原生业务嵌入 | 已接入口，不等于所有状态组合已验收 |
| 未知简单登记业务 | 字段方案→确认→创建→自动发现 | 隔离数据库完整生命周期19项通过；真实浏览器确认及创建表单保存待验收 |
| 未知复杂业务 | 源码扩展→测试→部署→业务视图 | 需提供可重复验证的扩展示例 |
| 非站点项目/外部服务 | 项目索引与 Codex 源码能力 | 逐项目辨识用途、认证和接入条件，不能假定已集成 |

不能以 957 个路由可生成宣布整个目标完成；逐项目/流程验收完成前目标保持进行中。

## 本批部署前证据

- 111 项站点上下文后端测试通过；每次结束回滚，激活测试使用隔离 mock，不在生产新建表。
- 前端联合回归 73 项通过，另包含 14 个状态断言；覆盖脏表单保留、失败与并发切换、蓝图显式确认及版本摘要。
- 957 个原生入口及 585 个权限允许的新建表单路由生成通过，0 失败；访客无法打开新建或蓝图接口。
- 使用真实 Frappe DocType.validate 与字段冲突校验，覆盖全部 10 种字段及最长名称；
  探针拦截非只读 SQL 和 commit，DocType/DocField/DocPerm 计数未变。
- 尚未以路由验证替代真实业务流程或新类型数据库生命周期验证。

## 部署检查点

2026-09-25 第一批已部署到 Native Bench，并推送 GitHub `develop`（实现提交 `0c1cb7f`，
部署预检 `66b73d9`）。Web/SSE/meal_chat 三服务 active；公网版本化资源返回 200，访客新建视图请求 403。
生产代码重新执行 111 项回归与真实原生只读 schema 校验通过。没有新建生产业务类型或真实业务记录。
真实浏览器验证默认周食谱、统一工作台标题、7 应用目录以及“已显示”前端回执通过。
浏览器操作较慢且偶发超时，原生表单/蓝图完整页面验收仍待继续；不能用这次目录验收替代它。

下一批优先：隔离环境验证方案→建表→新建/更新→权限→重新发现完整生命周期；
再完善分餐实际确认和出勤的场景内办理，按本账本逐领域核对未验收事项。

## 第二批：实际办理与隔离生命周期（2026-09-25）

- `classroom_day` 已增加出勤登记表，复用原 `classroom.save_attendance`；只提交明确变更，保留班级范围、revision、未来日期及锁定校验。
- 生效请假行只读，必须先用原请假流程撤假/返校，不能显示“改为到园”却实际仍请假。
- `meal_counts` 选择班级后在左侧办理本餐，原班级日单不变，新服务仅改当前餐；实际确认不覆盖此前预计。
- 新快照五餐均为未确认，预计晚餐0不再推定“不供餐”；历史记录不批量改写。
- 当前餐完整才给实际总数，未完成返回null；日汇总混合实际/预计时明确标识，全部完成后自动确认，无需每日二次确认。
- 大班可主动点“本班本餐全部就餐”填草稿，再改例外；不自动保存。名单区独立滚动，保存失败先回读，不盲目重试。
- 蓝图CLI在初始化Frappe前切换到站点服务Unix身份，私有方案使用0600，避免root生成的文件妨碍Web服务；Codex父进程执行能力不变。

### 已验证证据

- 专属空数据库和Redis位于loopback，站点 `unified-business-acceptance.localhost`；不复制生产数据或凭据。
- 蓝图真实19项（run `ff9098fc0e`）：任务绑定、私有方案、摘要确认、实际DDL、十种字段、原生必填校验、保存/修改/Version、目录发现、角色拒绝、重新连接回读。
- 出勤/分餐真实34项（run `0afbba8af3`）：3名合成学生、出勤/请假/纠正/撤假、stale/未来日期拒绝、分餐预计实际分离、变更原因、未确认保留、日汇总自动完成、锁定、审计、无权限拒绝和新连接回读。
- 真实允许路径为Administrator；无角色账号拒绝路径也已验证，不能据此宣称全部教师角色已验收。
- 193项后端单元回归、94项前端测试通过（状态测试另含14个内部断言）。
- 生产数据只读探针覆盖3班今日/未来出勤和用餐登记视图；拦截非只读SQL及commit，4类业务记录数量不变，未输出学生姓名。
- 真实浏览器已核实原生固定资产台账在左侧加载，右侧会话保留；没有模拟资产交易或保存。

### 仍需继续

1. 新增组件的隔离浏览器保存；教师授课范围正向生命周期（正式界面的只读验收已完成，见下）。
2. 采购→收货→库存及财务/HR/教育等专业状态链的隔离完整流程；原生入口不等于业务验收。
3. 复杂未知业务的可重复源码扩展示例，而非简单字段表冒充专业流程。
4. 31个目录的用途/接入条件已记录在 `project-business-coverage.md`；未接入独立服务仍需逐项认证和接口验证。
5. 审计发现原生Education考勤与课堂受限服务规则不同；当前主入口复用后者，未修改上游应用或声称所有旧入口已统一。

本批没有修改生产业务记录、创建生产自定义类型或变更已有用户权限。完整目标仍保持进行中。

### 第二批上线结果

实现提交 `04eb136` 已部署并推送 GitHub `develop`。Web、SSE、meal_chat worker均active，
版本化前端资源返回200；生产代码再次运行193项后端回归和只读业务探针通过。

真实浏览器只读验收：

- 出勤表在左侧显示30条分页记录与状态选择器，未修改时保存禁用，名单区独立滚动。
- 用餐汇总显示3班未确认、实际为未知（不是0）。进入一班早餐后80名学生均未确认，
  “本班本餐全部就餐”和“确认本餐”入口存在，未填写时确认禁用。
- 右侧会话持续可见；没有点击批量填值、保存、确认等生产写入动作。

上述界面验收与隔离ORM保存证据分开记录：不能声称已经通过真实浏览器提交端到端测试。

## 第三批：教师权限闭环、采购链路与受控复杂新业务（2026-09-25）

本批仍沿用“右侧会话、左侧业务视图、原业务规则”的同一主线。没有创建生产示例类型、
修改生产业务记录、给既有账号加角色，或修改上游 ERPNext 源码。

### 已实现

- 教师出勤原先被全园 Daily 汇总 DocPerm 挡住。现由既有受限点名服务核对启用账号、
  授课班级、学生、批次、日期与锁定后更新出勤事实及内部派生汇总；教师仍不能通过
  原生 Form/resource API 读写全园汇总。没有给 Instructor 全局 Daily 权限。
- 修复缺失的 `meal_attendance_roles.install` hook 为幂等无写入兼容模块；不自动授权。
- 新业务蓝图 v2：最多2明细表、每表12字段/500行、固定乘法与汇总、可选原生复核。
  左侧完整预览字段、公式、角色和流转，摘要绑定明确确认后才安装新结构。
- 服务端重新计算、拒绝负数/非有限/超限值与小数整数；保留修改历史，待复核/通过/
  撤销内容不可直接改。普通管理者不能自审；Administrator 的原生例外明示。
- 元数据清单独立于提出者私有文件，第二管理者能办理；每次写入核验父子表/Workflow。
  结构或流程漂移、失效/跨单据/重复明细行阻止写入；真实物理结构核验绕开元数据缓存。
  v2采用独立 `Tongjianyun Advanced ` 父类型命名空间，不能移除表格/提交标记后伪装为v1跳过校验。
- 原生 Workflow 及其状态/转换子行的表单/REST 变更有守卫；HTTP workflow 入口在原生
  条件/任务执行之前核验，防止先产生副作用再报错。特权 SQL/root 源码修改不在此保证内。
- Codex 的业务工具说明补充新模板格式、原生 mapper 重建 Document、库存数量及金额回读要求。

### 真实隔离证据（不同层次不混算）

| 场景 | 最新 run | 结果与边界 |
| --- | --- | --- |
| 真实教师身份的出勤/分餐/跨班/停用/原生汇总权限 | `9680e04d9f` | 54项；新连接回读；原锁定日逐字未变 |
| PO→分批收货→退货→取消→库存、权限及精确视图 | `c5c971c156` | 41项，**包含原生修复验证，不代表无缺陷** |
| v2明细计算→送审→退回→另一管理者通过→撤销 | `1396eb03c2` | 22项；真实DDL及原生ORM/Workflow |
| v2跨单明细、无效数值、权限、缺表/缺列/结构漂移/降级 | `10305e0151` | 57项；全部恢复自身测试结构后新连接回读 |
| Workflow主/子元数据与HTTP入口前置守卫 | `3fe566c2f3` | 35项；native handler＋spy，**未走HTTP网络** |
| v1登记蓝图兼容回归 | `b1837c7c3a` | 19项；v1完整生命周期仍通过 |

242项通用后端单元回归、101项前端测试通过。教师相关五模块另有127项隔离回归，
与通用回归有重叠，不相加宣传。全部JSON证据保存在专属隔离root；未把生产数据或凭据放入Git。

### 采购发现不能隐藏

当前 ERPNext 取消收货/退货后，Bin数量正确但库存价值可能滞后，原生重估任务即使
Completed也复现。已验证原生 `Bin.recalculate_values()` 可以恢复，保留修复前后证据；
本轮未在生产修复库存值或改ERP源码。详见 `deploy/unified_business/PROCUREMENT_STOCK_ACCEPTANCE.md`。
此外退货mapper结果应按原网页序列化后重建Document再保存，避免遗漏构造时的退货状态更新器。

### 未完成与下一批

1. 隔离浏览器真实保存（不是仅组件测试/ORM）。启动方案见 `isolated-browser-acceptance-plan.md`；
   尚未启动QA Web，不复制生产Cookie，不向真实Codex宿主runner发送测试任务。
2. 普通教师仍不能进入高权限Codex统一场景。受限教师服务已经验证，但需要另行设计
   业务视图与高权限会话的身份分离，不能把给教师System Manager当作接入。
3. 采购原生取消的库存价值缺陷需后续受测适配器/上游修复；尚未覆盖批次/有效期、供应商付款、
   多币种税务、并发、多级审批。财务、HR等专业状态链仍逐域验收。
4. 31目录包含基础设施和工具，非31个业务系统。独立应用认证和接口未全接入，7个已安装
   Frappe应用的957入口只是可发现/可打开，不等于957完整业务已验收。
5. v2是受控明细/估算/复核模板，不是任意业务生成器。外部集成、库存/会计过账等仍须复用
   原服务或另做受测试源码扩展。

完整目标保持进行中；本批不据测试数量宣布“所有业务已完成”。

### 第三批上线核验

实现提交 `d54dd50` 已快进部署至服务器。部署前确认无活动会话，仅平滑重启本功能
Web/SSE/meal_chat worker三项服务；三者均active。生产源码再次通过242项回归及只读
业务探针（3班出勤/分餐、未来实际禁用、四类业务记录数未变）。
公网版本化 `chat.js?v=unified-business-20260925-3` 返回200且引用新版views模块。
没有运行生产migrate、创建生产测试类型、修改生产业务记录或提交被排除的下载包。
本批未声称真实浏览器保存已经通过，下一批按上文隔离方案继续。
